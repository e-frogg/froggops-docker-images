name: fops-clock

on:
  push:
    branches: ["main"]
    paths:
      - "fops-clock/**"
      - ".github/workflows/_docker-image.yml"
      - ".github/workflows/fops-clock.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "fops-clock/**"
      - ".github/workflows/_docker-image.yml"
      - ".github/workflows/fops-clock.yml"

concurrency:
  group: fops-clock-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fops-clock/src
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: "1.21"
          cache-dependency-path: fops-clock/src/go.sum

      - name: Run unit tests
        run: go test ./...

  build:
    needs: test
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
      security-events: write
    uses: ./.github/workflows/_docker-image.yml
    with:
      image: fops-clock
      version: "1"
      context: ./fops-clock
      dockerfile: ./fops-clock/Dockerfile
      trivy-category: trivy-fops-clock
